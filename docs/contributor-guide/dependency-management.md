# Управление зависимостями

В VLMHyperBench используется принцип **автономных пакетов** и **динамической композиции**. Мы сознательно **не используем `uv workspaces`** в корневом проекте для обеспечения полной изоляции компонентов.

## 1. Команда `uv add`

Поскольку каждый пакет автономен, зависимости добавляются непосредственно внутри директории пакета.

### 1.1 Зависимость для конкретного пакета
```bash
cd packages/metric_registry
uv add numpy>=1.26
```

### 1.2 Глобальные зависимости (Playground)
В корневом репозитории (`vlmhyperbench_playground`) зависимости добавляются только для инструментов разработки и оркестратора.
```bash
uv add docker tqdm
```

## 2. Динамическое управление в контейнерах

Установка зависимостей внутри Docker-контейнеров происходит "на лету" (Just-in-Time) с помощью модуля `DependencyInjector`.

### 2.1 Как это работает
1. Оркестратор анализирует конфигурацию запуска (`RunTask`).
2. `DependencyInjector` формирует команду установки на основе выбранного `package_manager` (`pip`, `uv`, `conda`).
3. Зависимости устанавливаются в контейнер перед запуском основного скрипта.

### 1.4 Optional dependencies (Extras)
```bash
uv add flash-attn --package model-qwen2_5-vl --optional flash
```
Такие зависимости устанавливаются только при явном указании extra: `uv sync --extra flash`.

## 2. Ручное редактирование

1. Добавьте пакет в секцию `dependencies` нужного `pyproject.toml`.
2. Выполните синхронизацию из корня:
   ```bash
   uv lock
   uv sync
   ```

## 3. Обновление зависимостей

Для обновления всех пакетов до последних разрешенных версий:
```bash
uv lock --upgrade
uv sync
```

---

> 💡 **Совет**: Всегда проверяйте `uv.lock` после добавления зависимостей и коммитьте его вместе с `pyproject.toml`.